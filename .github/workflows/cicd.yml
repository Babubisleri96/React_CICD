name: "Sample react react pipeline"
on:
  branches:
    - main
    - master
jobs:
  build_test:
    runs-on: ubuntu-latest
    steps:
      - name: setup node js
        uses: actions/setup-node@v4
        with:
          node-version: '21'

